# Workforce descriptive peer

`elixir scripts/run_workforce_eda.exs WORKFORCE_CSV PRIVATE_OUTPUT RELEASE_REF_JSON` implements EDA and largest-remainder allocation independently of Python. It consumes the same canonical 51-region input and emits `workforce-eda.v1.json`, `analysis-run.v2.json` and `eda.md`.

The standalone script uses the OTP JSON module and requires the repository's declared Erlang/Elixir toolchain. Neither runtime was installed on the implementation host, so execution and format acceptance remain pending. Do not describe the implemented script as verified parity.

Python and Elixir remain peers. Selected replication proves this descriptive method; it does not require every future model twice. The unavailable regional v2 ensemble remains a separate blocked contract. No cloud credentials or source acquisition are needed for this script.

## Adoption contract — foundation 0.2.0

| Field | Contract |
|---|---|
| Layer | Elixir peer research |
| Ownership | Independent descriptive replication and selected research models. Source cleaning belongs to the economic pipeline. |
| Upstream | The same verified release used by Python, with explicit materialization binding. |
| Downstream | Reviewed scenario mapping and independently hashed analysis artifacts. |
| Supported entrypoints | `elixir scripts/run_workforce_eda.exs CSV OUT PIN.json materialization.json`; canonical Mix gates use the pinned supported container runtime. |
| Acceptance | Script implemented; runtime execution and dependency gate remain required. No parity success claimed. |
| Shared version | Foundation 0.2.0 candidate; `data-artifact.v1`, `verified-materialization.v1`, `analysis-run.v2` where applicable |

GCS is durable custody; bounded local paths are explicit materializations. The private foundation hub owns infrastructure, migration/recovery and integration runbooks. Public CI must remain credential-free and use synthetic contract fixtures; private release acceptance additionally pins coordinated commits, the wheel hash and contract checksums. Existing source IDs, historical timestamps and the fixed regional bundle are preserved.

Historical receipt v1 remains readable evidence. Publisher-backed acceptance requires v2 byte binding and a trusted pinned release; arbitrary CSV plus a cloud-shaped JSON reference is insufficient. Qualification, validation, durability, freshness and consumer eligibility remain separate.

See the [repository README](../README.md) for canonical gates and existing domain runbooks.

The [shared snapshot](../contracts/data-foundation/checksums.json) is checked by `python3 scripts/check_foundation_contracts.py` in credential-free CI. The private integration gate compares these checksums against the authoritative release.
